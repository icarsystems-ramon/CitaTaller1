{{#paper-list}}
    {{#paper-subheader class="md-no-sticky"}}Escoja lo más conveniente:{{/paper-subheader}}
    {{#each model as |job|}}
        {{#paper-item  param=job action="toggleJob"}}    
            {{paper-icon icon="build" icon-class="action"}}        
            <p>{{job.descripcion}}</p>
            {{paper-switch class="md-secondary" checked=job.selected value=job action="toggleJob"}}
        {{/paper-item}}
    {{/each}}
	
{{/paper-list}}
