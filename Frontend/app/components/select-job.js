import Ember from 'ember';

export default Ember.Component.extend({

    //actions: {
    //    toggleJob: function(job) {
    //        console.log ('Debug: Component. ToggleJob ' + job.get('descripcion'));
    //        this.sendAction('toggleJob',job);           
    //        return true;
    //    }
    //}
});
